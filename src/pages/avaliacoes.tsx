import { Link } from "react-router-dom";

const avaliacoes = () => {
  return (
    <div>
     <Link to="/login">Ir para Login</Link>
      <p>Profile Page</p>
    </div>
  );
};

export default avaliacoes;
