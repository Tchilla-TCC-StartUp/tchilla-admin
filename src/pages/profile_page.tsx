import { Link } from "react-router-dom";

const ProfilePage = () => {
  return (
    <div>
     <Link to="/login">Ir para Login</Link>
      <p>Profile Page</p>
    </div>
  );
};

export default ProfilePage;
