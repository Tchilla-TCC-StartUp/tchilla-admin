import { Link } from "react-router-dom";

const Categorias = () => {
  return (
    <div>
     <Link to="/login">Ir para Login</Link>
      <p>Profile Page</p>
    </div>
  );
};

export default Categorias;